version: '3.8'
services:
  db:
    image: 'mysql:8.4.2'
    container_name: mysql
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=1234
    ports:
      - '3306:3306'
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql